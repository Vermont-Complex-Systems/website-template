<!-- <script>
    let {
        xScale,
        innerHeight
    } = $props();

</script>

{#each xScale.ticks() as tick}
    <line
        x1={xScale(tick)}
        x2={xScale(tick)}
        y1={90}
        y2={innerHeight+10}
        stroke="lightgrey"
        stroke-width="1"
    />
{/each} -->
